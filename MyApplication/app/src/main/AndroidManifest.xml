<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tegdev.optotypes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashBoardActivity" />
        <activity android:name=".CrudSaveAppointmentActivity" />
        <activity android:name=".CrudModifyAppointmentActivity" />
        <activity android:name=".CrudDeleteAppointmentActivity" />
        <activity android:name=".InteractionActivity" />
        <activity android:name=".ResultInteractionActivity" />
        <activity android:name=".CrudReadAppointmentActivity" />
        <activity android:name=".CrudRequestTestActivity" />
        <activity android:name=".TestFormActivity" />
        <activity android:name=".DiagnosticActivity"></activity>
    </application>

</manifest>